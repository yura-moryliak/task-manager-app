import { TaskStatePipe } from './task-state.pipe';

describe('TaskStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskStatePipe();
    expect(pipe).toBeTruthy();
  });
});
